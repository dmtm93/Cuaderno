[build]
  command = "npm run build"
  publish = "dist"

# Cuaderno is a single-page app with no client-side routing today, but this
# redirect is harmless to keep in place and future-proofs against adding any.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
